id: sqli

info:
  name: sqli
  author: dat-ayush
  severity: critical
  description: Detects all types of potential sqli
  tags: sqli

http:
  - method: GET
    path:
      - "{{BaseURL}}"
    redirects: true
    max-redirects: 3

    extractors:
      - type: dsl
        name: original_body_length
        dsl:
          - "len(body)"

  - method: GET
    
    payloads:
      comments:
       - "'-- "
       - "'#"
       - "'--"
       - "'/*"

    fuzzing:
      - part: query
        type: postfix
        mode: single
        fuzz:
          - "{{comments}}"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - "len(body) == {{
    


    
